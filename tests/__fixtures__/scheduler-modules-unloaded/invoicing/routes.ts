/**
 * The file the DEFAULT autoload list does import — so the negative case proves
 * the module directory was reached and only the scheduler file was skipped.
 */
export const invoicingRoutesLoaded = true
